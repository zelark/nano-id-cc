{
  "name": "nano-id-cc",
  "version": "0.1.0",
  "private": true,
  "description": "NanoID Collision Calculator",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "dev": "esbuild public/css/style.css --minify --outfile=public/css/style.min.css && esbuild src/main.ts --bundle --watch --servedir=public --outdir=public/js"
  },
  "dependencies": {
    "nanoid": "^6.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
